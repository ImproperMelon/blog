language: node_js
sudo: false
node_js:
- 8
branches:
  only:
  - master
before_install:
- sed -i'' "s~https://github.com/ImproperMelon/ImproperMelon.github.io.git~https://${ACCESS_TOKEN}@github.com/ImproperMelon/ImproperMelon.github.io.git~" _config.yml
- git config --global user.name 'ImproperMelon'
- git config --global user.email 2422048218@qq.com
install:
- npm install hexo-cli -g
- npm install
script:
- npm run deploy
